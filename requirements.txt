# This project uses Node.js and npm for package management.
# While 'requirements.txt' is typically used for Python, 
# here are the primary dependencies found in the package.json files.
# To install all dependencies, simply run `npm install` in the root, server, and client directories.

Node.js >= 18.x

# Root Dependencies
concurrently==9.2.1

# Server Dependencies (./server)
cors==2.8.6
express==5.2.1
ffprobe-static==3.1.0
fluent-ffmpeg==2.1.3

# Client Dependencies (./client)
lucide-react==0.575.0
react==19.2.0
react-dom==19.2.0
vite==7.3.1
